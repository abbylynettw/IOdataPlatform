# 调试与部署配置说明

## 📋 三种场景配置

本文档提供三种场景的配置文件，直接复制使用即可。

---

## 🔧 场景一：本地开发调试

**适用场景**：开发调试、Bug 修复、功能开发

### 客户端配置
**文件**：`IODataPlatform/Assets/Settings/appsettings.Development.json`

```json
{
  "ConnectionStrings": {
    "SqlSugarContext": "Data Source=localhost;User ID=sa;Password=123456;Connect Timeout=3;Encrypt=False;Trust Server Certificate=False;Application Intent=ReadWrite;Multi Subnet Failover=False;Database=IOdb",
    "SqlSugarContext2": "Data Source=localhost;Initial Catalog=t2;User ID=sa;Password=123"
  },
  "WebServiceConfig": {
    "FileBaseUrl": "http://localhost:5041",
    "Key": "123456"
  },
  "OtherPlatFormConfig": {
    "ZentaoUrl": "http://10.33.11.60:81/zentao"
  }
}
```

**说明**：
- `FileBaseUrl`: 指向本地 WebApi，端口 5041
- 数据库使用 localhost
- 密钥使用简单密钥 123456

---

### 服务端配置
**文件**：`IODataPlatform.WebApi/appsettings.json`

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "FileServiceConfig": {
    "LocalRootDir": "C:\\Temp\\IOFiles",
    "Key": "123456"
  }
}
```

**说明**：
- `LocalRootDir`: 文件存储在本地临时目录
- `Key`: 必须与客户端一致

---

## 🏢 场景二：本地 IIS 部署

**适用场景**：本地测试 IIS 部署、模拟生产环境

### 客户端配置
**文件**：`IODataPlatform/Assets/Settings/appsettings.Development.json`

```json
{
  "ConnectionStrings": {
    "SqlSugarContext": "Data Source=localhost;User ID=sa;Password=123456;Connect Timeout=3;Encrypt=False;Trust Server Certificate=False;Application Intent=ReadWrite;Multi Subnet Failover=False;Database=IOdb",
    "SqlSugarContext2": "Data Source=localhost;Initial Catalog=t2;User ID=sa;Password=123"
  },
  "WebServiceConfig": {
    "FileBaseUrl": "http://localhost:3345",
    "Key": "123456"
  },
  "OtherPlatFormConfig": {
    "ZentaoUrl": "http://10.33.11.60:81/zentao"
  }
}
```

**说明**：
- `FileBaseUrl`: 指向本地 IIS，端口 3345
- 数据库使用 localhost
- 密钥使用简单密钥 123456

---

### 服务端配置
**文件**：`F:\IIS_Publish\IOPlatformWebApi\appsettings.json`

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "FileServiceConfig": {
    "LocalRootDir": "F:\\IIS_Publish\\IO管理软件文件资源库",
    "Key": "123456"
  }
}
```

**说明**：
- `LocalRootDir`: 文件存储在 IIS 发布目录
- `Key`: 必须与客户端一致

---

## 🌐 场景三：生产环境部署

**适用场景**：服务器部署 + 客户端分发到各机器

### 客户端配置（安装在各机器上）
**文件**：`IODataPlatform/Assets/Settings/appsettings.Production.json`

```json
{
  "ConnectionStrings": {
    "SqlSugarContext": "Data Source=10.33.11.60;User ID=sa;Password=Sa123456;Connect Timeout=10;Encrypt=False;Initial Catalog=IOdb",
    "SqlSugarContext2": "Data Source=10.33.11.60;Initial Catalog=t2;User ID=sa;Password=123"
  },
  "WebServiceConfig": {
    "FileBaseUrl": "http://10.33.11.60:3345",
    "Key": "123456"
  },
  "OtherPlatFormConfig": {
    "ZentaoUrl": "http://10.33.11.60:81/zentao"
  }
}
```

**说明**：
- `FileBaseUrl`: 指向服务器 IP 和端口
- 数据库连接使用服务器 IP
- **生产环境建议使用强密钥**（32位以上随机字符串）

---

### 服务端配置（服务器上）
**文件**：`F:\IIS_Publish\IOPlatformWebApi\appsettings.json`

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "FileServiceConfig": {
    "LocalRootDir": "F:\\IIS_Publish\\IO管理软件文件资源库",
    "Key": "123456"
  }
}
```

**说明**：
- `LocalRootDir`: 服务器文件存储目录
- `Key`: 必须与所有客户端一致
- **生产环境建议使用强密钥**

---

## 🔑 重要说明

### API 密钥 (Key)
- **开发/测试**：`123456`（简单密钥）
- **生产环境**：建议使用 32 位以上强密钥
- **关键要求**：客户端和服务端必须完全一致

### 端口说明
- **5041**：本地调试 WebApi
- **3345**：IIS 部署 WebApi
- **1433**：SQL Server 默认端口

### 文件存储路径
- **调试**：`C:\Temp\IOFiles`
- **生产**：`F:\IIS_Publish\IO管理软件文件资源库`
- **注意**：路径使用双反斜杠 `\\` 或单斜杠 `/`

---

## 📊 配置对照表

| 场景 | 客户端 FileBaseUrl | 服务端 LocalRootDir | 数据库地址 |
|------|-------------------|---------------------|-----------|
| 场景一 | `http://localhost:5041` | `C:\Temp\IOFiles` | `localhost` |
| 场景二 | `http://localhost:3345` | `F:\IIS_Publish\IO管理软件文件资源库` | `localhost` |
| 场景三 | `http://10.33.11.60:3345` | `F:\IIS_Publish\IO管理软件文件资源库` | `10.33.11.60` |

---

**最后更新：2025-10-23**
