<UserControl x:Class="IODataPlatform.Views.SubPages.Export.DraggableColumnPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="DraggableColumnItemStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DragPreviewStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Opacity" Value="0.8"/>
        </Style>

        <Style x:Key="DropIndicatorStyle" TargetType="Rectangle">
            <Setter Property="Fill" Value="{DynamicResource AccentFillColorDefaultBrush}"/>
            <Setter Property="Width" Value="3"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Opacity" Value="0.8"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <ui:SymbolIcon Symbol="ArrowSort24" FontSize="16" VerticalAlignment="Center"/>
            <ui:TextBlock Text="列顺序配置（拖拽调整）" FontWeight="Medium" Margin="8,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 列排序区域 -->
        <Border Grid.Row="1" 
                Background="{DynamicResource LayerFillColorDefaultBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <Canvas x:Name="ColumnCanvas" MinHeight="100">
                    <!-- 列项目将在这里动态添加 -->
                </Canvas>
            </ScrollViewer>
        </Border>

        <!-- 拖拽预览层 -->
        <Canvas x:Name="DragPreviewCanvas" Grid.RowSpan="2" 
                IsHitTestVisible="False" 
                Visibility="Collapsed">
            <!-- 拖拽预览项目 -->
            <Border x:Name="DragPreview" Style="{StaticResource DragPreviewStyle}">
                <ui:TextBlock x:Name="DragPreviewText" FontWeight="Medium"/>
            </Border>
        </Canvas>

        <!-- 投放指示器 -->
        <Canvas x:Name="DropIndicatorCanvas" Grid.Row="1" 
                IsHitTestVisible="False" 
                Visibility="Collapsed">
            <Rectangle x:Name="DropIndicator" Style="{StaticResource DropIndicatorStyle}"/>
        </Canvas>
    </Grid>
</UserControl>
