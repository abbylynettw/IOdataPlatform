<Page 
    x:Class="IODataPlatform.Views.Pages.DepYJPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:ly="http://lysoft/wpf"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:IODataPlatform.Views.Pages"
    d:DataContext="{d:DesignInstance local:DepYJPage, IsDesignTimeCreatable=False}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Page.Resources>
        <ly:BooleanToVisiblityConverter x:Key="BooleanToVisiblityConverter"/>
        <ly:NullToBooleanConverter IsInverse="True" x:Key="NullToBooleanConverter"/>
        <Style TargetType="ui:MenuItem"
               BasedOn="{StaticResource UiMenuItem}"
               x:Key="EditConfigurationTableMenuItem">
            <Setter Property="Command"
                    Value="{Binding ViewModel.EditConfigurationTableCommand}" />
            <Setter Property="CommandParameter"
                    Value="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Header}" />
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Menu>
                <ui:MenuItem Header="项目"
                             Icon="{ui:SymbolIcon MultiSelectRtl24}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="项目"
                                   VerticalAlignment="Center"
                                   Width="50" />
                        <ComboBox ItemsSource="{Binding ViewModel.Projects}"
                                  SelectedItem="{Binding ViewModel.Project}"
                                  Width="150">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="专业"
                                   VerticalAlignment="Center"
                                   Width="50" />
                        <ComboBox ItemsSource="{Binding ViewModel.Majors}"
                                  SelectedItem="{Binding ViewModel.Major}"
                                  Width="150">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CableSystem}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="子项"
                                   VerticalAlignment="Center"
                                   Width="50" />
                        <ComboBox ItemsSource="{Binding ViewModel.SubProjects}"
                                  SelectedItem="{Binding ViewModel.SubProject}"
                                  Width="150">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                    <Separator />
                    <ui:MenuItem Header="添加子项"
                                 Icon="{ui:SymbolIcon Add24}"
                                 Command="{Binding ViewModel.AddSubProjectCommand}" />
                    <ui:MenuItem Header="删除子项"
                                 Icon="{ui:SymbolIcon Delete24}"
                                 Command="{Binding ViewModel.DeleteSubProjectCommand}" />
                    <ui:MenuItem Header="全部刷新"
                                 Icon="{ui:SymbolIcon ArrowSync12}"
                                 Command="{Binding ViewModel.RefreshAllCommand}" />
                </ui:MenuItem>
                <Separator />
            </Menu>
            <Menu>
                <ui:MenuItem Header="配置表"
                             Icon="{ui:SymbolIcon TableSettings28}">
                    <ui:MenuItem Header="典回配置表"
                                 Style="{StaticResource EditConfigurationTableMenuItem}" />
                </ui:MenuItem>
                <ui:MenuItem Header="导入内部接线清单"
                             Icon="{ui:SymbolIcon TableMoveRight28}"
                             Command="{Binding ViewModel.GenerateDuanjieCommand}" />
                <ui:MenuItem Header="发布"
                             Icon="{ui:SymbolIcon Send24}"
                             Command="{Binding ViewModel.PublishCommand}" />
            </Menu>
        </StackPanel>
      
        <Menu Grid.Row="1" IsEnabled="{Binding ViewModel.AllData, Converter={StaticResource NullToBooleanConverter}}">
            <ui:MenuItem Header="添加" Icon="{ui:SymbolIcon AddCircle24}" Command="{Binding ViewModel.AddCommand}"/>
            <ui:MenuItem Header="刷新" Icon="{ui:SymbolIcon ArrowSync12}" Command="{Binding ViewModel.RefreshCommand}"/>
            <ui:MenuItem Header="显示列"
                         Icon="{ui:SymbolIcon Settings24}">
                <ScrollViewer MaxHeight="400">
                    <StackPanel Name="ColumnsVisibilityPanel" />
                </ScrollViewer>
            </ui:MenuItem>
            <ui:MenuItem Header="全部删除"
                         Icon="{ui:SymbolIcon Delete24}"
                         Command="{Binding ViewModel.ClearCommand}" />
            <ui:MenuItem Header="导入"
                         Icon="{ui:SymbolIcon Delete24}"
                         Command="{Binding ViewModel.ImportCommand}" />
            <Separator />
            <ui:MenuItem IsEnabled="False" Foreground="{DynamicResource TextFillColorPrimaryBrush}">
                <ui:MenuItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <ui:TextBlock Text="总行数：" FontSize="14" />
                        <ui:TextBlock Margin="5,0,0,0" Text="{Binding ViewModel.AllDataCount}" FontSize="14" />
                    </StackPanel>
                </ui:MenuItem.Header>
            </ui:MenuItem>
        </Menu>

        <ui:DataGrid x:Name="grid1" Margin="0,0,0,10" Grid.Row="2" ItemsSource="{Binding ViewModel.AllData}" AutoGenerateColumns="False"
                     CanUserAddRows="False" FrozenColumnCount="1" Style="{StaticResource DataGridStyle}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="106"
                                        Header="操作">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <ui:Button Content="编辑"
                                           CommandParameter="{Binding}"
                                           Command="{Binding ViewModel.EditCommand, RelativeSource={RelativeSource AncestorType=local:DepYJPage}}" />
                                <ui:Button Appearance="Danger"
                                           Icon="{ui:SymbolIcon Delete24}"
                                           CommandParameter="{Binding}"
                                           Command="{Binding ViewModel.DeleteCommand, RelativeSource={RelativeSource AncestorType=local:DepYJPage}}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </ui:DataGrid>
        
    </Grid>
</Page>