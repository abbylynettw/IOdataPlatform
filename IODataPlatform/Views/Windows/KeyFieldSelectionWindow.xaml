<ui:FluentWindow x:Class="IODataPlatform.Views.Windows.KeyFieldSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="选择主键字段"
        Height="500"
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 搜索框 -->
        <ui:TextBox Grid.Row="0"
                    x:Name="FieldSearchBox"
                    PlaceholderText="搜索字段..."
                    Icon="Search24"
                    TextChanged="FieldSearchBox_TextChanged"
                    Margin="0,0,0,10" />

        <!-- 操作按钮栏 -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <ui:Button Content="按顺序对比"
                       Icon="ArrowSort24"
                       Click="OrderMode_Click"
                       Margin="0,0,5,0" />
            <ui:Button Content="取消全选"
                       Icon="CheckboxUnchecked24"
                       Click="UnselectAll_Click"
                       Margin="0,0,5,0" />
        </WrapPanel>

        <!-- 字段列表 -->
        <Border Grid.Row="2"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <UniformGrid x:Name="FieldsPanel" Columns="3" />
            </ScrollViewer>
        </Border>

        <!-- 底部按钮 -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="#666666">
                <Run Text="已选择: " />
                <Run x:Name="SelectedCountText" Text="0" FontWeight="Bold" />
                <Run Text=" 个字段" />
            </TextBlock>

            <ui:Button Grid.Column="1"
                       Content="确定"
                       Appearance="Primary"
                       Click="Confirm_Click"
                       Width="100"
                       Margin="0,0,10,0" />
            
            <ui:Button Grid.Column="2"
                       Content="取消"
                       Click="Cancel_Click"
                       Width="100" />
        </Grid>
    </Grid>
</ui:FluentWindow>
