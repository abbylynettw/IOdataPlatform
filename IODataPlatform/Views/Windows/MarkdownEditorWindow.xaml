<ui:FluentWindow x:Class="IODataPlatform.Views.Windows.MarkdownEditorWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             Title="Markdown 编辑器"
             Width="1400"
             Height="800"
             WindowStartupLocation="CenterOwner"
             ExtendsContentIntoTitleBar="True"
             WindowBackdropType="Mica">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Grid.Row="0" Background="{DynamicResource ApplicationBackgroundBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="15,10">
                <ui:TextBlock x:Name="TitleText" 
                              Text="编辑文档" 
                              FontSize="16" 
                              FontWeight="SemiBold"
                              VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10">
                <ui:Button x:Name="BtnInsertImage"
                           Content="插入图片"
                           Icon="{ui:SymbolIcon Image24}"
                           Appearance="Secondary"
                           Margin="0,0,10,0"/>
            </StackPanel>
        </Grid>

        <!-- 编辑器和预览区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧编辑器 -->
            <Border Grid.Column="0" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}">
                <wv2:WebView2 x:Name="EditorWebView"/>
            </Border>

            <!-- 分隔条 -->
            <GridSplitter Grid.Column="1" 
                          HorizontalAlignment="Stretch" 
                          VerticalAlignment="Stretch"
                          Background="{DynamicResource ControlStrokeColorDefaultBrush}"/>

            <!-- 右侧预览 -->
            <Border Grid.Column="2" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}">
                <wv2:WebView2 x:Name="PreviewWebView"/>
            </Border>
        </Grid>

        <!-- 底部按钮栏 -->
        <Border Grid.Row="2" 
                Background="{DynamicResource ApplicationBackgroundBrush}"
                BorderThickness="0,1,0,0" 
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="15,10">
                <ui:Button x:Name="BtnSave"
                           Content="保存"
                           Icon="{ui:SymbolIcon Save24}"
                           Appearance="Primary"
                           Margin="0,0,10,0"/>
                <ui:Button x:Name="BtnCancel"
                           Content="取消"
                           Icon="{ui:SymbolIcon Dismiss24}"
                           Appearance="Secondary"/>
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
