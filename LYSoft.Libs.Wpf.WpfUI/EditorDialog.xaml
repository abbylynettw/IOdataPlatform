<ui:FluentWindow x:Class="LYSoft.EditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Name="TitleBar"/>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="1">
            <StackPanel Name="Body" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20"/>
        </ScrollViewer>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="20,0,40,20">
            <ui:Button Name="OkButton" HorizontalAlignment="Stretch" Click="OK_Click" Appearance="Primary" MinWidth="100"/>
            <ui:Button Name="CancelButton" Margin="10,10,0,10" HorizontalAlignment="Stretch" Click="Cancel_Click" MinWidth="100"/>
        </StackPanel>
    
    </Grid>

</ui:FluentWindow>
